# ──────────────────────────────────────────────────────
#  SDD Configuration — Cabañas Catamarca
#  Auto-generated by sdd-init on 2026-05-22
# ──────────────────────────────────────────────────────

project: cabanas
description: "App Cabañas Catamarca — reservas online con frontend HTML/CSS/JS y backend Google Apps Script"
persistence_mode: openspec

# ── Project Context ──────────────────────────────────
context:
  stack: "Vanilla HTML5/CSS3/JS frontend + Google Apps Script backend (V8 runtime)"
  build: "Node.js html-minifier-terser for minification (node minify.js)"
  deploy:
    frontend: "GitHub Pages via master→main sync workflow"
    backend: "Google Apps Script via clasp push"
  conventions:
    - "Project memory in AGENTS.md (Spanish)"
    - "Backend changes require manual GAS re-deployment"
    - "Frontend changes auto-deploy via GitHub Actions"
    - "PowerShell deploy script: desplegar.ps1"
  languages:
    code: "JavaScript (ES6+), Google Apps Script (JS V8)"
    documentation: "Spanish"
  has_ci: false

# ── Strict TDD ──────────────────────────────────────
strict_tdd: false
strict_tdd_reason: "No test runner detected. npm test is a placeholder (echo + exit 1). No test files, no test dependencies, no test framework installed."

# ── Testing Capabilities ────────────────────────────
testing:
  runner:
    exists: false
    command: "npm test"
    framework: null
    note: "Placeholder only — no test framework installed"
  layers:
    unit:
      available: false
      tool: null
    integration:
      available: false
      tool: null
    e2e:
      available: false
      tool: null
  coverage:
    available: false
    command: null
  quality:
    linter:
      available: false
      command: null
    type_checker:
      available: false
      command: null
    formatter:
      available: false
      command: null

# ── SDD Phase Rules ─────────────────────────────────
phases:
  propose:
    format: intent-scope-approach
    max_file_scan: 10
  spec:
    format: delta-spec
    scenarios_required: true
  design:
    format: technical-design
    diagrams: optional
  tasks:
    max_task_lines: 300
    include_tests: when_strict_tdd
  apply:
    validate_with_tests: false
    commit_style: conventional
  verify:
    test_required: false
  archive:
    persist_delta: true
